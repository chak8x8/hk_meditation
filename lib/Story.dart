import 'audio.dart';
import 'package:flutter/material.dart';

//class Story extends Audio{
  //int chapter;

 // Story({required title, required length, required narrator, required language}): super(title: title, length: length, narrator: narrator, language: language);
  //Story({required title, required length, required narrator, required language, required this.chapter}): super(title: title, length: length, narrator: narrator, language: language);

//}